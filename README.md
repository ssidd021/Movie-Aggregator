# Movie-Aggregator
A simple web application in Javascript, jQuery and Axios using HTTP and OMDb API. 
User can search for movies by name and view a list of search results retrieved by OMDb API and displayed using a Bootswatch stylesheet.
Uses OMDb API to fetch results. 

